<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/head') %>
</head>

<body id="page-top">

    <div id="wrapper">

        <%- include('partials/sidebar', { page: 'loans' }) %>

        <div id="content-wrapper" class="d-flex flex-column">
            <div id="content">

                <nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">
                    <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
                        <i class="fa fa-bars"></i>
                    </button>
                    <h1 class="h3 mb-0 text-gray-800">Control de Préstamos</h1>
                </nav>

                <div class="container-fluid">

                    <div class="row">
                        <div class="col-xl-3 col-md-6 mb-4">
                            <div class="card border-left-warning shadow h-100 py-2">
                                <div class="card-body">
                                    <div class="row no-gutters align-items-center">
                                        <div class="col mr-2">
                                            <div class="text-xs font-weight-bold text-warning text-uppercase mb-1">
                                                Herramientas Prestadas</div>
                                            <div class="h5 mb-0 font-weight-bold text-gray-800"><%= loans.length %></div>
                                        </div>
                                        <div class="col-auto">
                                            <i class="fas fa-tools fa-2x text-gray-300"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        
                        <div class="col-lg-5">
                            <div class="card shadow mb-4 border-bottom-primary">
                                <div class="card-header py-3 bg-primary">
                                    <h6 class="m-0 font-weight-bold text-white"><i class="fas fa-hand-holding mr-2"></i>Registrar Salida</h6>
                                </div>
                                <div class="card-body">
                                    <form action="/loans/add" method="POST">
                                        
                                        <% if (typeof error !== 'undefined' && error) { %>
                                            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                                <%= error %>
                                                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                                    <span aria-hidden="true">&times;</span>
                                                </button>
                                            </div>
                                        <% } %>

                                        <div class="form-group">
                                            <label class="font-weight-bold small text-primary">EMPLEADO</label>
                                            <div class="input-group">
                                                <div class="input-group-prepend">
                                                    <span class="input-group-text bg-light border-0"><i class="fas fa-user text-gray-500"></i></span>
                                                </div>
                                                <input type="text" name="employeeName" class="form-control bg-light border-0 small" placeholder="Nombre completo o Gafete..." required>
                                            </div>
                                        </div>

                                        <hr>

                                        <div class="form-group">
                                            <label class="font-weight-bold small text-primary">CÓDIGO HERRAMIENTA (QR)</label>
                                            <div class="input-group">
                                                <div class="input-group-prepend">
                                                    <span class="input-group-text bg-light border-0"><i class="fas fa-qrcode text-gray-500"></i></span>
                                                </div>
                                                <input type="text" name="productCode" class="form-control bg-light border-0 small" placeholder="Escanear..." required autofocus>
                                            </div>
                                        </div>

                                        <button type="submit" class="btn btn-primary btn-block btn-icon-split mt-4">
                                            <span class="icon text-white-50">
                                                <i class="fas fa-arrow-right"></i>
                                            </span>
                                            <span class="text">Prestar Herramienta</span>
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>

                        <div class="col-lg-7">
                            <div class="card shadow mb-4">
                                <div class="card-header py-3">
                                    <h6 class="m-0 font-weight-bold text-primary">En Resguardo (Sin devolver)</h6>
                                </div>
                                <div class="card-body">
                                    <div class="table-responsive">
                                        <table class="table table-bordered" width="100%" cellspacing="0">
                                            <thead class="bg-gray-200 text-gray-900">
                                                <tr>
                                                    <th>Herramienta / Clave</th> <th>Responsable</th>
                                                    <th>Fecha</th>
                                                    <th>Hora</th> <th>Acción</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% loans.forEach(loan => { %>
                                                    <tr>
                                                        <td>
                                                            <div class="font-weight-bold text-dark"><%= loan.product ? loan.product.description : '???' %></div>
                                                            <% if (loan.product && loan.product.short_code) { %>
                                                                <span class="badge badge-secondary"><%= loan.product.short_code %></span>
                                                            <% } else { %>
                                                                <small class="text-muted">Sin clave</small>
                                                            <% } %>
                                                        </td>
                                                        <td class="align-middle">
                                                            <i class="fas fa-user-circle mr-1 text-gray-400"></i>
                                                            <%= loan.employee ? loan.employee.name : 'Desc.' %>
                                                        </td>
                                                        <td class="align-middle">
                                                            <%= new Date(loan.date_out).toLocaleDateString() %>
                                                        </td>
                                                        <td class="align-middle text-primary font-weight-bold">
                                                            <%= new Date(loan.date_out).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %>
                                                        </td>
                                                        <td class="text-center align-middle">
                                                            <form action="/loans/return/<%= loan.id %>" method="POST">
                                                                <button type="submit" class="btn btn-success btn-sm btn-icon-split shadow-sm" title="Registrar Devolución">
                                                                    <span class="icon text-white-50">
                                                                        <i class="fas fa-undo-alt"></i>
                                                                    </span>
                                                                    <span class="text">Devolver</span>
                                                                </button>
                                                            </form>
                                                        </td>
                                                    </tr>
                                                <% }) %>

                                                <% if(loans.length === 0) { %>
                                                    <tr>
                                                        <td colspan="5" class="text-center py-4 text-gray-500">
                                                            <i class="fas fa-check-circle fa-2x mb-2 text-gray-300"></i><br>
                                                            No hay herramientas prestadas.
                                                        </td>
                                                    </tr>
                                                <% } %>
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </div>
                </div>
                </div>
            <footer class="sticky-footer bg-white">
                <div class="container my-auto">
                    <div class="copyright text-center my-auto">
                        <span>Copyright &copy; Tu Empresa 2026</span>
                    </div>
                </div>
            </footer>

        </div>
    </div>

    <%- include('partials/scripts') %>

</body>
</html>